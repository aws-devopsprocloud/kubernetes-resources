apiVersion: apps/v1 
kind: Deployment
metadata: 
  name: deploymentset  # Deploymentset name 
  labels: 
    app: nginx-deploymentset 
    tier: frontend 
spec: 
  replicas: 2 
  selector:
    matchLabels: # replicaset labels 
      app: nginx-deploymentset  
      tier: frontend 

  template: # Pod definition 
    metadata:
      name: deploymentset-demo # Pod name# ACTUAL POD NAME WILL BE DIFFERENT AFTER Kubectl apply
      labels:
        app: nginx-deploymentset 
        tier: frontend 
    spec: 
      containers:
      - name: nginx # container name 
        image: nginx:stable-alpine3.23-perl
        ports: 
        - containerPort: 80 

