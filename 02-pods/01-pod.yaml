apiVersion: v1 
kind: Pod 
metadata: 
  name: sample-pod 
  # namespace: roboshop # We can give the name space 
spec:
  containers: 
  - name: nginx 
    image: nginx 
    ports:
    - containerPort: 80 